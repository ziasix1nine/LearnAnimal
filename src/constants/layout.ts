/** Shared sizing so every screen keeps the same toddler-friendly touch targets. */
export const layout = {
  minTouchTarget: 88,
  radiusLarge: 32,
  radiusMedium: 24,
  radiusSmall: 16,
  spacing: {
    xs: 6,
    sm: 12,
    md: 20,
    lg: 28,
    xl: 40,
  },
} as const;
